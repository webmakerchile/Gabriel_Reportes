Camino A. EB Garamond + EB Garamond SC + IBM Plex Sans. Procede.
El razonamiento que diste es correcto: italic 400 ↔ roman 800 en Garamond ya entrega el contraste de pesos que pedí, sin sintetizar pesos que no fueron dibujados. Cormorant tiene x-height más baja, italic menos refinado y la familia SC no dedicada — los tres son trade-offs reales que se notarían en el resultado final. No vale la pena cambiar la fuente correcta para conseguir un peso 300 que en la práctica no aporta más que el italic ya bien pesado.
Una sola precisión antes de que arranques con el plan, para que no me la pases por alto:
EB Garamond SC para versalitas reales — perfecto, eso es exactamente lo que pedí. Pero verifica un detalle: en Google Fonts, EB Garamond SC viene en pesos discretos (400, 500, 600, 700, 800) y no como variable como su hermana. Asegúrate de que cuando aplicas versalitas en distintos contextos — kickers tipo "DÍA 12", "FASE II · VALLE", labels de stats — el peso elegido se mantenga consistente. Sugiero 600 para casi todos los kickers (tiene cuerpo suficiente para no desaparecer en parchment/40) y reserva 400 solo para versalitas dentro de bloques de texto corrido si llegas a necesitarlas (no creo que aparezcan en este diseño). Documenta esto en los tokens de index.css como una clase utilitaria (.smallcaps con su peso fijo) para que no termine variando entre componentes.
Y un par de cosas adicionales del lado de carga, ya que las mencionaste:

El peso italic 500 que listaste — confirma que lo vas a usar realmente. Si no aparece en el diseño final, sácalo del subset. 11 archivos es manejable pero cada uno que no se use es ~15 KB de aire.
font-display: swap en todo, sí, pero preload solo el peso que aparece en el LCP de la home. En este diseño es el H1 "Reto de Oración 21 Días" en EB Garamond 800 — solo ese woff2 va con <link rel="preload">. El resto carga normal. Esto evita FOUT en el elemento que más importa visualmente sin penalizar la carga total.
Subset latin-ext está bien para español (cubre acentos, ñ, comillas españolas «»). No cargues el subset cyrillic ni vietnamese aunque Google los ofrezca — peso muerto.

Confirmadas las fuentes y esos detalles de carga, procede con el plan completo: tokens, ornamentos SVG, refactor de WelcomeScreen, ParticipantDashboard (con ink-fill y odómetro de racha) y admin (login, sidebar 220px, KPI hairline, tablas).
Una última cosa: cuando termines la implementación, antes del test de validación de 5 preguntas que te dejé en el prompt original, mándame screenshots reales (o descripciones detalladas si no puedes) de los tres bloques que mockeaste — home, day block, KPI admin. Quiero verificar que el resultado renderizado coincide con la intención editorial antes de declararlo terminado, porque la diferencia entre "se ve bien en CSS" y "se ve bien en pantalla" suele ser donde se cuela el último 10% del look genérico.
Adelante.